function degreesToRadians(degrees) {
    // Convert degrees to radians
    return degrees * (Math.PI / 180);
  }
  
  // Example usage:
  let degrees = 45;
  let radians = degreesToRadians(degrees);
  console.log(radians);  // Output: 0.7853981633974483 (which is π/4)
  