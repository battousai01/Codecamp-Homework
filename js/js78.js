function calculateCircumference(radius) {
    // Calculate the circumference using the formula: 2 * π * r
    return 2 * Math.PI * radius;
  }
  
  // Example usage:
  let radius = 5;
  let circumference = calculateCircumference(radius);
  console.log(circumference);  // This will log the circumference of a circle with radius 5
  